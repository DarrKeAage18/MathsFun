{
  "name": "fun-maths",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=public/bundle.js --sourcemap",
    "dev": "esbuild src/main.ts --bundle --outfile=public/bundle.js --sourcemap --watch",
    "serve": "serve ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "serve": "^14.2.6",
    "typescript": "^5.0.0"
  }
}
